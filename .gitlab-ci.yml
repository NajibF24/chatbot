variables:
  # Folder custom
  GIT_CLONE_PATH: '$CI_BUILDS_DIR/$CI_CONCURRENT_ID/project-baru-bersih'
  GIT_CLEAN_FLAGS: -ffdx -e data/
  
  # Definisi Nama Image
  SERVER_IMAGE: $CI_REGISTRY_IMAGE/server:latest
  CLIENT_IMAGE: $CI_REGISTRY_IMAGE/client:latest
  
  # URL Website untuk DAST Scan
  TARGET_URL: "https://portalai.gyssteel.com"
  
  # PENTING: Agar SonarQube bisa mendeteksi "New Code" vs "Old Code"
  GIT_DEPTH: "0"

stages:
  - security      # Trivy (Scanning Library/OS)
  - code_quality  # SonarQube (Scanning Kualitas Kodingan) <--- BARU
  - build         # Build & Push
  - deploy        # Deploy
  - dast          # OWASP ZAP (Attack Website)
  - report        # Upload ke DefectDojo

# --- JOB 1: TRIVY SCAN ---
security_scan:
  stage: security
  tags:
    - portal-ai
  allow_failure: true
  script:
    - echo "🛡️ Memulai Deep Security Scanning..."
    - wget https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/html.tpl -O trivy-report.tpl
    
    # 1. SCAN SERVER
    - echo "🔍 Deep Scan Server..."
    - trivy fs --scanners vuln,secret,misconfig,license --format template --template "@trivy-report.tpl" -o report-server.html ./server
    - trivy fs --format cyclonedx -o sbom-server.json ./server
    - trivy fs --scanners vuln,secret,misconfig,license --format json -o trivy-server.json ./server
    
    # 2. SCAN CLIENT
    - echo "🔍 Deep Scan Client..."
    - trivy fs --scanners vuln,secret,misconfig,license --format template --template "@trivy-report.tpl" -o report-client.html ./client
    - trivy fs --scanners vuln,secret,misconfig,license --format json -o trivy-client.json ./client

  artifacts:
    name: "Deep-Security-Reports-$CI_COMMIT_SHORT_SHA"
    paths:
      - report-server.html
      - trivy-server.json
      - sbom-server.json
      - report-client.html
      - trivy-client.json
    expire_in: 7 days
    when: always

# --- JOB BARU: SONARQUBE CHECK ---
# --- UPDATE JOB: SONARQUBE CHECK (SHELL RUNNER FRIENDLY) ---
# --- FIX: SONARQUBE CHECK (SINGLE LINE COMMAND) ---
sonarqube_check:
  stage: code_quality
  tags:
    - portal-ai
  variables:
    GIT_DEPTH: "0"
  script:
    - echo "🧠 Memulai Analisa Code Quality (via Docker Container)..."
    
    # PERBAIKAN: Perintah dijalankan dalam satu baris memanjang agar tidak error parsing
    - docker run --rm -e SONAR_HOST_URL="${SONAR_HOST_URL}" -e SONAR_TOKEN="${SONAR_TOKEN}" -v "$(pwd):/usr/src" sonarsource/sonar-scanner-cli -Dsonar.projectKey=portal-ai -Dsonar.sources=. -Dsonar.exclusions="**/node_modules/**,**/dist/**,**/build/**"

  allow_failure: true
  only:
    - main
    
# --- JOB 3: BUILD & PUSH ---
build_push_images:
  stage: build
  tags:
    - portal-ai
  only:
    - main
  before_script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin $CI_REGISTRY
  script:
    - echo "🏗️ Building & Pushing Images..."
    - echo "$DOT_ENV_FILE" > .env
    - docker build -t $SERVER_IMAGE ./server
    - docker push $SERVER_IMAGE
    - docker build --build-arg REACT_APP_API_URL=${REACT_APP_API_URL} -t $CLIENT_IMAGE ./client
    - docker push $CLIENT_IMAGE

# --- JOB 4: DEPLOY ---
deploy_production:
  stage: deploy
  tags:
    - portal-ai
  only:
    - main
  needs: ["build_push_images"]
  before_script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin $CI_REGISTRY    
  script:
    - echo "🚀 Memulai Deployment..."
    - echo "$DOT_ENV_FILE" > .env
    - docker rm -f mongo internal-chat-server internal-chat-client || true
    - docker compose down --remove-orphans || true
    - docker pull $SERVER_IMAGE
    - docker pull $CLIENT_IMAGE
    - docker compose up -d 
    - sleep 10
    - docker image prune -f

# --- JOB 5: DAST (ZAP) ---
dast_scan:
  stage: dast
  tags:
    - portal-ai
  allow_failure: true
  script:
    - echo "🕵️ Memulai DAST Scanning..."
    - mkdir -p zap-reports
    - chmod 777 zap-reports
    - docker pull ghcr.io/zaproxy/zaproxy:stable
    
    # PASSIVE SCAN (Aman untuk Production)
    - docker run --rm -v $(pwd)/zap-reports:/zap/wrk/:rw -t ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t $TARGET_URL -r zap-report.html -x zap-report.xml -I
    
  artifacts:
    name: "DAST-Report-$CI_COMMIT_SHORT_SHA"
    paths:
      - zap-reports/zap-report.html
      - zap-reports/zap-report.xml
    expire_in: 7 days
    when: always
    
# --- JOB 6: UPLOAD REPORT TO DEFECTDOJO ---
upload_to_defectdojo:
  stage: report
  tags:
    - portal-ai
  image: curlimages/curl
  allow_failure: true
  script:
    - echo "🚀 Mengirim Laporan ke Server Infra (DefectDojo)..."
    
    # Upload Trivy SERVER
    - |
      curl -X POST "http://172.16.21.82:8080/api/v2/import-scan/" \
      -H "Authorization: Token $DEFECT_DOJO_API_KEY" \
      -H "Content-Type: multipart/form-data" \
      -F "scan_type=Trivy Scan" \
      -F "file=@trivy-server.json" \
      -F "product_name=Portal AI" \
      -F "engagement_name=GitLab CI Pipeline" \
      -F "auto_create_context=true" \
      -F "close_old_findings=true"

    # Upload Trivy CLIENT
    - |
      curl -X POST "http://172.16.21.82:8080/api/v2/import-scan/" \
      -H "Authorization: Token $DEFECT_DOJO_API_KEY" \
      -H "Content-Type: multipart/form-data" \
      -F "scan_type=Trivy Scan" \
      -F "file=@trivy-client.json" \
      -F "product_name=Portal AI" \
      -F "engagement_name=GitLab CI Pipeline" \
      -F "auto_create_context=true" \
      -F "close_old_findings=true"

    # Upload ZAP DAST
    - |
      curl -X POST "http://172.16.21.82:8080/api/v2/import-scan/" \
      -H "Authorization: Token $DEFECT_DOJO_API_KEY" \
      -H "Content-Type: multipart/form-data" \
      -F "scan_type=ZAP Scan" \
      -F "file=@zap-reports/zap-report.xml" \
      -F "product_name=Portal AI" \
      -F "engagement_name=GitLab CI Pipeline" \
      -F "auto_create_context=true" \
      -F "close_old_findings=true"